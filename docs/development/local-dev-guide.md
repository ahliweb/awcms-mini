# Local Development Guide

Quick start:

- `pnpm install`
- configure local env vars
- `pnpm dev` for Astro host
- `pnpm dev:api` for Hono API

Validation commands:

- `pnpm test:unit`
- `pnpm typecheck`
- `pnpm lint`
