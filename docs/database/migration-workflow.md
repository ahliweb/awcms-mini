# Migration Workflow

Use forward-only migrations with Kysely helpers.

Commands:

- `pnpm db:migrate`
- `pnpm db:migrate:status`
- `pnpm db:migrate:emdash:verify`

Deployment checklist reference:

- `docs/process/migration-deployment-checklist.md`
