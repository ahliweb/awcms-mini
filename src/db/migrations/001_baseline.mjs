/**
 * Baseline migration bootstrap.
 *
 * This intentionally does not create business tables yet. It establishes a
 * first tracked migration so the runner can be exercised on an empty database.
 */

export async function up() {}

export async function down() {}
