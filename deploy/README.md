# Deployment Assets

Issue 12.2 menambahkan deployment profile lengkap. Folder ini sudah disiapkan pada Issue 0.1 untuk systemd, container, PgBouncer, backup, dan dokumen operator.

Semua deployment harus menjalankan runtime Bun, bukan Node.js.
