{
  "name": "awcms-mini",
  "version": "0.9.0",
  "private": true,
  "description": "AWCMS-Mini — Modular Monolith Standard (Bun + Astro foundation skeleton)",
  "license": "MIT",
  "author": "AhliWeb",
  "homepage": "https://github.com/ahliweb/awcms-mini#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ahliweb/awcms-mini.git"
  },
  "bugs": {
    "url": "https://github.com/ahliweb/awcms-mini/issues"
  },
  "keywords": [
    "modular-monolith",
    "bun",
    "astro",
    "postgresql",
    "multi-tenant",
    "rbac",
    "abac",
    "rls",
    "offline-first",
    "standard"
  ],
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "dev": "bun --bun astro dev",
    "build": "bun --bun astro build",
    "preview": "bun --bun astro preview",
    "start": "bun ./dist/server/entry.mjs",
    "db:migrate": "bun scripts/db-migrate.ts",
    "api:spec:check": "bun scripts/api-spec-check.ts",
    "lint": "prettier --check \"**/*.md\" \"**/*.{json,yml,yaml}\" \"**/*.{ts,mjs,astro}\"",
    "format": "prettier --write \"**/*.md\" \"**/*.{json,yml,yaml}\" \"**/*.{ts,mjs,astro}\"",
    "check:docs": "bun scripts/check-docs.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "check": "bun run lint && bun run check:docs && bun run api:spec:check && bun run typecheck && bun run test && bun run build",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:status": "changeset status",
    "changeset:tag": "changeset tag"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.0",
    "prettier": "^3.8.3",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^6.0.3",
    "yaml": "^2.9.0"
  },
  "dependencies": {
    "@astrojs/node": "^11.0.2",
    "astro": "^7.0.0"
  }
}
