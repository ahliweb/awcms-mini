diff --git a/src/astro/middleware.ts b/src/astro/middleware.ts
index 545e018fb2eb61ed25382abcb922eeadbb1599d2..7597181e92c92b85feb093b258a80814ee0b174e 100644
--- a/src/astro/middleware.ts
+++ b/src/astro/middleware.ts
@@ -51,6 +51,11 @@ import type { EmDashHandlers } from "./types.js";
 let runtimeInstance: EmDashRuntime | null = null;
 // Whether initialization is in progress (prevents concurrent init attempts)
 let runtimeInitializing = false;
+// Epoch ms when the current initialization attempt started.
+let runtimeInitializingSince = 0;
+
+const RUNTIME_INIT_WAIT_MS = 3000;
+const RUNTIME_INIT_POLL_MS = 50;
 
 /** Whether i18n config has been initialized from the virtual module */
 let i18nInitialized = false;
@@ -138,14 +143,31 @@ async function getRuntime(config: EmDashConfig): Promise<EmDashRuntime> {
 
 	// If another request is already initializing, wait and retry.
 	// We don't share the promise across requests because workerd flags
-	// cross-request promise resolution (causes warnings + potential hangs).
+	// cross-request promise resolution. Instead, poll briefly and give up on
+	// stale init attempts so requests fail fast rather than hanging forever.
 	if (runtimeInitializing) {
-		// Poll until the initializing request finishes
-		await new Promise((resolve) => setTimeout(resolve, 50));
-		return getRuntime(config);
+		const waitStartedAt = Date.now();
+
+		while (runtimeInitializing && !runtimeInstance) {
+			const initAge = runtimeInitializingSince > 0 ? Date.now() - runtimeInitializingSince : 0;
+			const waitAge = Date.now() - waitStartedAt;
+
+			if (initAge > RUNTIME_INIT_WAIT_MS || waitAge > RUNTIME_INIT_WAIT_MS) {
+				runtimeInitializing = false;
+				runtimeInitializingSince = 0;
+				break;
+			}
+
+			await new Promise((resolve) => setTimeout(resolve, RUNTIME_INIT_POLL_MS));
+		}
+
+		if (runtimeInstance) {
+			return runtimeInstance;
+		}
 	}
 
 	runtimeInitializing = true;
+	runtimeInitializingSince = Date.now();
 	try {
 		const deps = buildDependencies(config);
 		const runtime = await EmDashRuntime.create(deps);
@@ -153,6 +175,7 @@ async function getRuntime(config: EmDashConfig): Promise<EmDashRuntime> {
 		return runtime;
 	} finally {
 		runtimeInitializing = false;
+		runtimeInitializingSince = 0;
 	}
 }
 
@@ -182,9 +205,15 @@ export const onRequest = defineMiddleware(async (context, next) => {
 	// Process /_emdash routes and public routes with an active session
 	// (logged-in editors need the runtime for toolbar/visual editing on public pages)
	const isEmDashRoute = url.pathname.startsWith("/_emdash");
+	const isSetupShellRoute = url.pathname.startsWith("/_emdash/admin/setup");
+	const isSetupApiRoute = url.pathname.startsWith("/_emdash/api/setup");
 	const isPublicRuntimeRoute =
 		PUBLIC_RUNTIME_ROUTES.has(url.pathname) || SITEMAP_COLLECTION_RE.test(url.pathname);

+	if (isSetupShellRoute || isSetupApiRoute) {
+		const response = await next();
+		return setBaselineSecurityHeaders(response);
+	}
+
	// Check for edit mode cookie - editors viewing public pages need the runtime
	// so auth middleware can verify their session for visual editing
	const hasEditCookie = cookies.get("emdash-edit-mode")?.value === "true";

diff --git a/src/astro/routes/api/setup/status.ts b/src/astro/routes/api/setup/status.ts
index 5d6f2f0af1f65c2fb8ad0d53cb06afcfa722f31f..c13f7f74de4b9ee2837b81195c6ef7df1bfb7e62 100644
--- a/src/astro/routes/api/setup/status.ts
+++ b/src/astro/routes/api/setup/status.ts
@@ -8,17 +8,20 @@ import type { APIRoute } from "astro";
 
 export const prerender = false;
 
+import { sql } from "kysely";
-import { apiError, apiSuccess, handleError } from "#api/error.js";
+import { apiSuccess, handleError } from "#api/error.js";
 import { getAuthMode } from "#auth/mode.js";
 import { loadUserSeed } from "#seed/load.js";
+import { getDb } from "../../../../loader.js";
+
+function summarizeDirectDatabaseTarget(databaseUrl?: string) {
+	if (!databaseUrl) {
+		return {
+			transport: "direct",
+			source: "DATABASE_URL",
+			hostname: null,
+			port: null,
+			database: null,
+			sslmode: null,
+		};
+	}
+
+	try {
+		const parsed = new URL(databaseUrl);
+		return {
+			transport: "direct",
+			source: "DATABASE_URL",
+			hostname: parsed.hostname || null,
+			port: parsed.port ? Number(parsed.port) : 5432,
+			database: parsed.pathname.replace(/^\//, "") || null,
+			sslmode: parsed.searchParams.get("sslmode") || null,
+		};
+	} catch {
+		return {
+			transport: "direct",
+			source: "DATABASE_URL",
+			hostname: null,
+			port: null,
+			database: null,
+			sslmode: null,
+		};
+	}
+}
+
+function describeRuntimeDatabasePosture() {
+	if (process.env.DATABASE_TRANSPORT === "hyperdrive") {
+		return {
+			transport: "hyperdrive",
+			source: "Cloudflare Hyperdrive binding",
+			binding: process.env.HYPERDRIVE_BINDING || "HYPERDRIVE",
+		};
+	}
+
+	return summarizeDirectDatabaseTarget(process.env.DATABASE_URL);
+}
+
+function createRuntimeHealthPayload(databaseOk: boolean, reason: string | null = null) {
+	return {
+		database: {
+			ok: databaseOk,
+			...(databaseOk
+				? {}
+				: {
+					reason,
+				}),
+			posture: describeRuntimeDatabasePosture(),
+		},
+	};
+}
 
 export const GET: APIRoute = async ({ locals }) => {
 	const { emdash } = locals;
-
-	if (!emdash?.db) {
-		return apiError("NOT_CONFIGURED", "EmDash is not initialized", 500);
-	}
 
 	try {
+		const db = emdash?.db ?? (await getDb());
+		let setupCompleteValue: string | null = null;
+		let setupStateValue: string | null = null;
+		let runtimeHealth = createRuntimeHealthPayload(true);
 		let hasUsers = false;
+
+		try {
+			await sql`select 1`.execute(db);
+		} catch {
+			runtimeHealth = createRuntimeHealthPayload(false, "connection_error");
+		}
 
 		// Check if setup is complete
-		const setupComplete = await emdash.db
-			.selectFrom("options")
-			.select("value")
-			.where("name", "=", "emdash:setup_complete")
-			.executeTakeFirst();
+		try {
+			const setupComplete = await db.selectFrom("options").select("value").where("name", "=", "emdash:setup_complete").executeTakeFirst();
+			setupCompleteValue = setupComplete?.value ?? null;
+
+			const setupState = await db.selectFrom("options").select("value").where("name", "=", "emdash:setup_state").executeTakeFirst();
+			setupStateValue = setupState?.value ?? null;
+		} catch {
+			// Fresh environments may not have the options table yet.
+		}
 
 		// Value is JSON-encoded, parse it. Accepts both boolean true and string "true"
 		const isComplete =
-			setupComplete &&
+			setupCompleteValue &&
 			(() => {
 				try {
-					const parsed = JSON.parse(setupComplete.value);
+					const parsed = JSON.parse(setupCompleteValue);
 					return parsed === true || parsed === "true";
 				} catch {
 					return false;
@@ -42,7 +45,7 @@ export const GET: APIRoute = async ({ locals }) => {
 		// Also check if users exist
 		let hasUsers = false;
 		try {
-			const userCount = await emdash.db
+			const userCount = await db
 				.selectFrom("users")
 				.select((eb) => eb.fn.countAll<number>().as("count"))
 				.executeTakeFirstOrThrow();
@@ -62,14 +65,7 @@ export const GET: APIRoute = async ({ locals }) => {
 		// step: "start" | "site" | "admin" | "complete"
 		let step: "start" | "site" | "admin" = "start";
 
-		// Get setup state if it exists
-		const setupState = await emdash.db
-			.selectFrom("options")
-			.select("value")
-			.where("name", "=", "emdash:setup_state")
-			.executeTakeFirst();
-
-		if (setupState) {
+		if (setupStateValue) {
 			try {
-				const state = JSON.parse(setupState.value);
+				const state = JSON.parse(setupStateValue);
 				if (state.step === "admin") {
 					step = "admin";
 				} else if (state.step === "site") {
@@ -88,7 +84,7 @@ export const GET: APIRoute = async ({ locals }) => {
		}

		// Check auth mode
-		const authMode = getAuthMode(emdash.config);
+		const authMode = emdash?.config ? getAuthMode(emdash.config) : null;
+		const useExternalAuth = authMode?.type === "external";

@@ -100,7 +147,8 @@ export const GET: APIRoute = async ({ locals }) => {
		if (useExternalAuth && isComplete) {
			return apiSuccess({
				needsSetup: false,
+				runtimeHealth,
			});
		}

@@ -118,10 +166,11 @@ export const GET: APIRoute = async ({ locals }) => {
		return apiSuccess({
			needsSetup: true,
			step,
			seedInfo,
+			runtimeHealth,
			// Tell the wizard which auth mode is active
			authMode: useExternalAuth ? authMode.providerType : "passkey",
		});

diff --git a/src/astro/routes/api/setup/index.ts b/src/astro/routes/api/setup/index.ts
index 8fd8966cb9196b3667c026cc11c91c5079220254..7a855df66f8c69ca7d219ee9877cbefc0644551e 100644
--- a/src/astro/routes/api/setup/index.ts
+++ b/src/astro/routes/api/setup/index.ts
@@ -4,6 +4,8 @@
  */
 
 import type { APIRoute } from "astro";
+import virtualConfig from "virtual:emdash/config";
+import { createStorage as virtualCreateStorage } from "virtual:emdash/storage";
 
 export const prerender = false;
 
@@ -17,15 +19,18 @@ import { OptionsRepository } from "#db/repositories/options.js";
 import { applySeed } from "#seed/apply.js";
 import { loadSeed } from "#seed/load.js";
 import { validateSeed } from "#seed/validate.js";
+import { getDb } from "../../../../loader.js";
 
 export const POST: APIRoute = async ({ request, url, locals }) => {
 	const { emdash } = locals;
-
-	if (!emdash?.db) {
-		return apiError("NOT_CONFIGURED", "EmDash is not initialized", 500);
-	}
 
 	try {
+		const db = emdash?.db ?? (await getDb());
+		const config = emdash?.config ?? virtualConfig;
+		const storage =
+			emdash?.storage ??
+			(config?.storage && virtualCreateStorage ? virtualCreateStorage(config.storage.config) : undefined);
+
 		// Guard: reject if setup has already been completed.
 		// The options table may not exist on first-ever setup (pre-migration),
 		// so a query failure means setup hasn't run yet — allow it to proceed.
 		try {
-			const options = new OptionsRepository(emdash.db);
+			const options = new OptionsRepository(db);
 			const setupComplete = await options.get("emdash:setup_complete");
 
 			if (setupComplete === true || setupComplete === "true") {
@@ -47,7 +52,7 @@ export const POST: APIRoute = async ({ request, url, locals }) => {
 
 		// 1. Run core migrations
 		try {
-			await runMigrations(emdash.db);
+			await runMigrations(db);
 		} catch (error) {
 			return handleError(error, "Failed to run database migrations", "MIGRATION_ERROR");
 		}
@@ -70,17 +75,17 @@ export const POST: APIRoute = async ({ request, url, locals }) => {
 
 		let result;
 		try {
-			result = await applySeed(emdash.db, seed, {
+			result = await applySeed(db, seed, {
 				includeContent: body.includeContent,
 				onConflict: "skip",
-				storage: emdash.storage ?? undefined,
+				storage,
 			});
 		} catch (error) {
 			return handleError(error, "Failed to apply seed", "SEED_ERROR");
 		}
 
 		// 5. Store setup state
 		// In external auth mode, mark setup complete immediately (first user to login becomes admin)
 		// In passkey mode, setup_complete is set after admin user is created
-		const authMode = getAuthMode(emdash.config);
+		const authMode = getAuthMode(config);
 		const useExternalAuth = authMode.type === "external";
 
 		try {
-			const options = new OptionsRepository(emdash.db);
+			const options = new OptionsRepository(db);
 
 			// Store the canonical site URL from the setup request.
 			// This is trusted because setup runs on the real domain.
-			const siteUrl = getPublicOrigin(url, emdash.config);
+			const siteUrl = getPublicOrigin(url, config);
 			await options.set("emdash:site_url", siteUrl);

diff --git a/src/astro/routes/api/setup/admin.ts b/src/astro/routes/api/setup/admin.ts
index b9fbe05ce784815a4b5281c6e954260a5180d78f..58e2682ad5d0945f03f4bac88d8d2e7ece92150d 100644
--- a/src/astro/routes/api/setup/admin.ts
+++ b/src/astro/routes/api/setup/admin.ts
@@ -4,6 +4,7 @@
  */
 
 import type { APIRoute } from "astro";
+import virtualConfig from "virtual:emdash/config";
 
 export const prerender = false;
 
@@ -17,15 +18,15 @@ import { createChallengeStore } from "#auth/challenge-store.js";
 import { getPasskeyConfig } from "#auth/passkey-config.js";
 import { OptionsRepository } from "#db/repositories/options.js";
+import { getDb } from "../../../../loader.js";
 
 export const POST: APIRoute = async ({ request, locals }) => {
 	const { emdash } = locals;
-
-	if (!emdash?.db) {
-		return apiError("NOT_CONFIGURED", "EmDash is not initialized", 500);
-	}
 
 	try {
+		const db = emdash?.db ?? (await getDb());
+		const config = emdash?.config ?? virtualConfig;
+
 		// Check if setup is already complete
-		const options = new OptionsRepository(emdash.db);
+		const options = new OptionsRepository(db);
 		const setupComplete = await options.get("emdash:setup_complete");
 
 		if (setupComplete === true || setupComplete === "true") {
@@ -35,7 +36,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		}
 
 		// Check if any users exist
-		const adapter = createKyselyAdapter(emdash.db);
+		const adapter = createKyselyAdapter(db);
 		const userCount = await adapter.countUsers();
 
 		if (userCount > 0) {
@@ -55,11 +56,11 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		// Get passkey config
 		const url = new URL(request.url);
 		const siteName = (await options.get<string>("emdash:site_title")) ?? undefined;
-		const siteUrl = getPublicOrigin(url, emdash?.config);
+		const siteUrl = getPublicOrigin(url, config);
 		const passkeyConfig = getPasskeyConfig(url, siteName, siteUrl);
 
 		// Generate registration options
-		const challengeStore = createChallengeStore(emdash.db);
+		const challengeStore = createChallengeStore(db);
 
 		// Create a temporary user object for registration options
 		// (not persisted until passkey is verified)

diff --git a/src/astro/routes/api/setup/admin-verify.ts b/src/astro/routes/api/setup/admin-verify.ts
index 2102f733a4401722144c7573464d4767340e3927..6ec3710b45c083a6ca022f89fb8fcdd62a2c6476 100644
--- a/src/astro/routes/api/setup/admin-verify.ts
+++ b/src/astro/routes/api/setup/admin-verify.ts
@@ -5,6 +5,7 @@
  */
 
 import type { APIRoute } from "astro";
+import virtualConfig from "virtual:emdash/config";
 
 export const prerender = false;
 
@@ -18,15 +19,15 @@ import { createChallengeStore } from "#auth/challenge-store.js";
 import { getPasskeyConfig } from "#auth/passkey-config.js";
 import { OptionsRepository } from "#db/repositories/options.js";
+import { getDb } from "../../../../loader.js";
 
 export const POST: APIRoute = async ({ request, locals }) => {
 	const { emdash } = locals;
-
-	if (!emdash?.db) {
-		return apiError("NOT_CONFIGURED", "EmDash is not initialized", 500);
-	}
 
 	try {
+		const db = emdash?.db ?? (await getDb());
+		const config = emdash?.config ?? virtualConfig;
+
 		// Check if setup is already complete
-		const options = new OptionsRepository(emdash.db);
+		const options = new OptionsRepository(db);
 		const setupComplete = await options.get("emdash:setup_complete");
 
 		if (setupComplete === true || setupComplete === "true") {
@@ -36,7 +37,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		}
 
 		// Check if any users exist
-		const adapter = createKyselyAdapter(emdash.db);
+		const adapter = createKyselyAdapter(db);
 		const userCount = await adapter.countUsers();
 
 		if (userCount > 0) {
@@ -56,11 +57,11 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		// Get passkey config
 		const url = new URL(request.url);
 		const siteName = (await options.get<string>("emdash:site_title")) ?? undefined;
-		const siteUrl = getPublicOrigin(url, emdash?.config);
+		const siteUrl = getPublicOrigin(url, config);
 		const passkeyConfig = getPasskeyConfig(url, siteName, siteUrl);
 
 		// Verify the registration response
-		const challengeStore = createChallengeStore(emdash.db);
+		const challengeStore = createChallengeStore(db);

		const verified = await verifyRegistrationResponse(

diff --git a/dist/astro/middleware.mjs b/dist/astro/middleware.mjs
--- a/dist/astro/middleware.mjs
+++ b/dist/astro/middleware.mjs
@@
-	const isEmDashRoute = url.pathname.startsWith("/_emdash");
-	const isPublicRuntimeRoute = PUBLIC_RUNTIME_ROUTES.has(url.pathname) || SITEMAP_COLLECTION_RE.test(url.pathname);
+	const isEmDashRoute = url.pathname.startsWith("/_emdash");
+	const isSetupShellRoute = url.pathname.startsWith("/_emdash/admin/setup");
+	const isSetupApiRoute = url.pathname.startsWith("/_emdash/api/setup");
+	const isPublicEdgeHealthRoute = url.pathname === "/api/v1/health";
+	const isPublicRuntimeRoute = PUBLIC_RUNTIME_ROUTES.has(url.pathname) || SITEMAP_COLLECTION_RE.test(url.pathname);
+	if (isSetupShellRoute || isSetupApiRoute || isPublicEdgeHealthRoute) {
+		const response = await next();
+		return setBaselineSecurityHeaders(response);
+	}
 			passkeyConfig,
