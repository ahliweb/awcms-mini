# OpenAPI Contracts

Issue 0.3 menambahkan baseline OpenAPI lengkap. Folder ini sudah disiapkan pada Issue 0.1 agar struktur repository stabil.

Kontrak REST wajib mengikuti response helper di `src/modules/_shared/api-response.ts`.
