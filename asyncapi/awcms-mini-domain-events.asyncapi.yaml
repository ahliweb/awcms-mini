asyncapi: 3.0.0
info:
  title: AWCMS-Mini Domain Events
  version: 1.0.0
  description: >-
    Stable AsyncAPI contract for AWCMS-Mini domain events and sync
    envelopes (ADR-0008 — contract version is independent of the package
    release version; bumped only when the contract shape itself changes).
defaultContentType: application/json
servers:
  local:
    host: localhost:4321
    protocol: http
    description: Local AWCMS-Mini runtime.
channels:
  awcms-mini.sync.push.requested:
    address: awcms-mini.sync.push.requested
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: Baseline sync push event envelope for future sync-storage implementation.
  awcms-mini.database.pool.saturated:
    address: awcms-mini.database.pool.saturated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      Emitted when a work-class concurrency gate times out a queued caller
      (Issue 10.2, doc 16 §Connection pooling dan backpressure, doc 05 "DB
      Connectivity" category). Documented contract only — this repo has no
      live pub/sub dispatcher for any domain event yet; the concrete producer
      is the structured JSON logger (`src/lib/logging/logger.ts`) invoked
      from `withTenant` (`src/lib/database/tenant-context.ts`), consistent
      with how AsyncAPI events have been documented since Issue 0.3 without
      requiring a live producer.
  awcms-mini.database.pool.rejected:
    address: awcms-mini.database.pool.rejected
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      Emitted when a work-class concurrency gate's bounded FIFO queue is
      already full and a caller is rejected IMMEDIATELY, without ever
      waiting (Issue #743, epic #738 platform-evolution — distinct from
      `database.pool.saturated` above, which is a caller that waited and
      THEN timed out; doc 16 §Connection pooling dan backpressure, doc 05
      "DB Connectivity" category). Documented contract only, same
      convention as `database.pool.saturated` — the concrete producer is
      the structured JSON logger (`src/lib/logging/logger.ts`) invoked from
      `withTenant` (`src/lib/database/tenant-context.ts`) on a
      `WorkClassQueueFullError`.
  awcms-mini.email.message.queued:
    address: awcms-mini.email.message.queued
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      An email message was enqueued into `awcms_mini_email_messages` (Issue
      #494/#497). Documented contract only, same convention as
      `database.pool.saturated` above — the concrete producer is the
      structured JSON logger, invoked from
      `email/application/announcement-directory.ts`'s `enqueueAnnouncement`
      (`email.message.queued` log line).
  awcms-mini.email.message.sent:
    address: awcms-mini.email.message.sent
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      The email dispatcher (Issue #495, `bun run email:dispatch`)
      successfully delivered a message through the configured provider.
      Documented contract only; producer is the structured JSON logger
      (`email/application/email-dispatch.ts`'s `email.dispatch.sent` log
      line).
  awcms-mini.email.message.failed:
    address: awcms-mini.email.message.failed
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      The email dispatcher exhausted retries (or hit a non-retryable
      failure) for a queued message. Documented contract only; producer is
      the structured JSON logger (`email/application/email-dispatch.ts`'s
      `email.dispatch.failed` log line).
  awcms-mini.email.message.suppressed:
    address: awcms-mini.email.message.suppressed
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      The email dispatcher (Issue #499) found a claimed message's recipient
      newly present on `awcms_mini_email_suppression_list` (added after
      enqueue, before dispatch) and skipped the provider call entirely.
      Documented contract only; producer is the structured JSON logger
      (`email/application/email-dispatch.ts`'s `email.dispatch.suppressed`
      log line).
  awcms-mini.email.message.cancelled:
    address: awcms-mini.email.message.cancelled
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      An operator cancelled a still-queued message (Issue #499,
      `POST /api/v1/email/messages/{id}/cancel`) — the technical mitigation
      for the "accidental bulk send" incident scenario. Documented contract
      only; producer is the structured JSON logger, invoked from
      `pages/api/v1/email/messages/[id]/cancel.ts` (`email.message.cancelled`
      log line).
  awcms-mini.blog-content.post.created:
    address: awcms-mini.blog-content.post.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A blog post draft was created (Issue #538/#541). Documented contract
      only, same convention as the email channels above — producer is the
      structured JSON logger, invoked from
      `pages/api/v1/blog/posts/index.ts`'s `POST` handler
      (`blog-content.post.created` log line).
  awcms-mini.blog-content.post.updated:
    address: awcms-mini.blog-content.post.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A blog post was updated (Issue #538/#541). Documented contract only;
      producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/posts/[id].ts`'s `PATCH` handler
      (`blog-content.post.updated` log line).
  awcms-mini.blog-content.post.submitted-for-review:
    address: awcms-mini.blog-content.post.submitted-for-review
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A blog post moved from `draft`/`review` into `review` (Issue
      #538/#541). Documented contract only; producer is the structured JSON
      logger, invoked from
      `pages/api/v1/blog/posts/[id]/submit-review.ts`
      (`blog-content.post.submitted-for-review` log line).
  awcms-mini.blog-content.post.published:
    address: awcms-mini.blog-content.post.published
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A blog post transitioned to `published` (Issue #538/#541), either via
      the admin `POST /api/v1/blog/posts/{id}/publish` action or the
      `bun run blog:publish:scheduled` job publishing a due `scheduled`
      post. Documented contract only; producer is the structured JSON
      logger, invoked from `pages/api/v1/blog/posts/[id]/publish.ts` and
      `blog-content/application/blog-scheduled-publish.ts`
      (`blog-content.post.published` log line, `trigger` attribute
      distinguishes the two callers).
  awcms-mini.blog-content.post.scheduled:
    address: awcms-mini.blog-content.post.scheduled
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A blog post was scheduled for future publication (Issue #538/#541).
      Documented contract only; producer is the structured JSON logger,
      invoked from `pages/api/v1/blog/posts/[id]/schedule.ts`
      (`blog-content.post.scheduled` log line).
  awcms-mini.blog-content.post.archived:
    address: awcms-mini.blog-content.post.archived
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A blog post was archived (Issue #538/#541). Documented contract only;
      producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/posts/[id]/archive.ts`
      (`blog-content.post.archived` log line).
  awcms-mini.blog-content.post.deleted:
    address: awcms-mini.blog-content.post.deleted
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A blog post was soft-deleted (Issue #538/#541). Documented contract
      only; producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/posts/[id].ts`'s `DELETE` handler
      (`blog-content.post.deleted` log line).
  awcms-mini.blog-content.post.restored:
    address: awcms-mini.blog-content.post.restored
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A soft-deleted blog post was restored (Issue #538/#541 — not to be
      confused with `revision.created`, which fires when a *revision* is
      restored onto the live post). Documented contract only; producer is
      the structured JSON logger, invoked from
      `pages/api/v1/blog/posts/[id]/restore.ts`
      (`blog-content.post.restored` log line).
  awcms-mini.blog-content.post.purged:
    address: awcms-mini.blog-content.post.purged
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A blog post was hard-deleted (Issue #538/#541). Documented contract
      only; producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/posts/[id]/purge.ts`
      (`blog-content.post.purged` log line).
  awcms-mini.blog-content.revision.created:
    address: awcms-mini.blog-content.revision.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A new row was appended to `awcms_mini_blog_revisions` (Issue #541) —
      either a significant title/contentJson/contentText change on
      `PATCH /api/v1/blog/{posts,pages}/{id}`, or a revision restore
      (`POST /api/v1/blog/posts/{id}/revisions/{revisionId}/restore`)
      appending the restored snapshot. `awcms_mini_blog_revisions` is
      append-only — this event never corresponds to an `UPDATE`/`DELETE` of
      an existing revision row. Documented contract only; producer is the
      structured JSON logger, invoked from
      `blog-content/application/blog-revision-directory.ts`'s
      `createBlogRevision` (`blog-content.revision.created` log line).
  awcms-mini.blog-content.term.created:
    address: awcms-mini.blog-content.term.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A taxonomy term (category/tag) was created (Issue #539/#541).
      Documented contract only; producer is the structured JSON logger,
      invoked from `pages/api/v1/blog/terms/index.ts`'s `POST` handler
      (`blog-content.term.created` log line).
  awcms-mini.blog-content.term.updated:
    address: awcms-mini.blog-content.term.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A taxonomy term (category/tag) was updated (Issue #539/#541).
      Documented contract only; producer is the structured JSON logger,
      invoked from `pages/api/v1/blog/terms/[id].ts`'s `PATCH` handler
      (`blog-content.term.updated` log line).
  awcms-mini.blog-content.settings.updated:
    address: awcms-mini.blog-content.settings.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      Reserved for when `awcms_mini_blog_settings` (migration 026) gets a
      write route — the `blog_content.settings.configure` permission was
      seeded in Issue #537 but no endpoint consumes it yet. Documented
      contract only, no current producer (unlike every other channel in
      this module) — listed now per doc issue #541's required AsyncAPI
      event set so the contract is complete ahead of the route landing in a
      later issue.
  awcms-mini.blog-content.template.created:
    address: awcms-mini.blog-content.template.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A presentation template was created (Issue #542). Documented contract
      only; producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/templates/index.ts`'s `POST` handler
      (`blog-content.template.created` log line).
  awcms-mini.blog-content.template.updated:
    address: awcms-mini.blog-content.template.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A presentation template was updated (Issue #542). Documented contract
      only; producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/templates/[id].ts`'s `PATCH` handler
      (`blog-content.template.updated` log line).
  awcms-mini.blog-content.template.deleted:
    address: awcms-mini.blog-content.template.deleted
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A presentation template was soft-deleted (Issue #542). Documented
      contract only; producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/templates/[id].ts`'s `DELETE` handler
      (`blog-content.template.deleted` log line).
  awcms-mini.blog-content.menu.created:
    address: awcms-mini.blog-content.menu.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A navigation menu was created (Issue #542). Documented contract only;
      producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/menus/index.ts`'s `POST` handler
      (`blog-content.menu.created` log line).
  awcms-mini.blog-content.menu.updated:
    address: awcms-mini.blog-content.menu.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A navigation menu (or its item tree, full-replace) was updated
      (Issue #542). Documented contract only; producer is the structured
      JSON logger, invoked from `pages/api/v1/blog/menus/[id].ts`'s
      `PATCH` handler (`blog-content.menu.updated` log line).
  awcms-mini.blog-content.menu.deleted:
    address: awcms-mini.blog-content.menu.deleted
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A navigation menu was soft-deleted (Issue #542). Documented contract
      only; producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/menus/[id].ts`'s `DELETE` handler
      (`blog-content.menu.deleted` log line).
  awcms-mini.blog-content.widget.created:
    address: awcms-mini.blog-content.widget.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A widget was created (Issue #542). Documented contract only; producer
      is the structured JSON logger, invoked from
      `pages/api/v1/blog/widgets/index.ts`'s `POST` handler
      (`blog-content.widget.created` log line).
  awcms-mini.blog-content.widget.updated:
    address: awcms-mini.blog-content.widget.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A widget was updated (Issue #542). Documented contract only; producer
      is the structured JSON logger, invoked from
      `pages/api/v1/blog/widgets/[id].ts`'s `PATCH` handler
      (`blog-content.widget.updated` log line).
  awcms-mini.blog-content.widget.deleted:
    address: awcms-mini.blog-content.widget.deleted
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A widget was soft-deleted (Issue #542). Documented contract only;
      producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/widgets/[id].ts`'s `DELETE` handler
      (`blog-content.widget.deleted` log line).
  awcms-mini.blog-content.ad.created:
    address: awcms-mini.blog-content.ad.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      An advertisement was created (Issue #542). Documented contract only;
      producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/ads/index.ts`'s `POST` handler
      (`blog-content.ad.created` log line).
  awcms-mini.blog-content.ad.updated:
    address: awcms-mini.blog-content.ad.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      An advertisement (or its placement targeting, full-replace) was
      updated (Issue #542). Documented contract only; producer is the
      structured JSON logger, invoked from
      `pages/api/v1/blog/ads/[id].ts`'s `PATCH` handler
      (`blog-content.ad.updated` log line).
  awcms-mini.blog-content.ad.deleted:
    address: awcms-mini.blog-content.ad.deleted
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      An advertisement was soft-deleted (Issue #542). Documented contract
      only; producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/ads/[id].ts`'s `DELETE` handler
      (`blog-content.ad.deleted` log line).
  awcms-mini.blog-content.theme.updated:
    address: awcms-mini.blog-content.theme.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      The tenant's blog theme mode override was set (Issue #542) — a
      blog-scoped override of the base tenant theme engine
      (`awcms_mini_tenants.default_theme`, migration 002), not a
      replacement for it. Documented contract only; producer is the
      structured JSON logger, invoked from `pages/api/v1/blog/theme/
      index.ts`'s `PATCH` handler (`blog-content.theme.updated` log line).
  awcms-mini.blog-content.internal-tag-linking-policy.updated:
    address: awcms-mini.blog-content.internal-tag-linking-policy.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      This tenant's automatic internal tag linking policy (enabled,
      caseInsensitive, disabledTagIds — `awcms_mini_blog_internal_tag_link_
      settings`) was updated (Issue #641). Documented contract only;
      producer is the structured JSON logger, invoked from
      `pages/api/v1/blog/internal-tag-links/settings.ts`'s `PATCH` handler
      (`blog-content.internal-tag-linking-policy.updated` log line).
  awcms-mini.social-publishing.account.connected:
    address: awcms-mini.social-publishing.account.connected
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A social account was connected or reconnected/reauthorized (Issue
      #643). Documented contract only, same producer convention as every
      other event in this file — the structured JSON logger, invoked from
      `social-publishing/application/social-account-directory.ts`'s
      `connectSocialAccount` (`social_publishing.account.connected` audit
      event + log line).
  awcms-mini.social-publishing.account.disconnected:
    address: awcms-mini.social-publishing.account.disconnected
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A social account was disconnected (Issue #643) — a status
      transition, not a delete. Producer:
      `social-account-directory.ts`'s `disconnectSocialAccount`.
  awcms-mini.social-publishing.account.needs-reauth:
    address: awcms-mini.social-publishing.account.needs-reauth
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A connected social account's token expired/was rejected by its
      provider and now requires reauthorization (Issue #643). Producer:
      `social-account-directory.ts`'s `markSocialAccountNeedsReauth`,
      called by the outbox dispatcher on a `needs_reauth` publish outcome.
  awcms-mini.social-publishing.account.verified:
    address: awcms-mini.social-publishing.account.verified
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A connected social account was verified — the provider adapter
      confirmed the credential can post to the connected target (Issue
      #646, the Telegram channel adapter's `verifyCredentials`; also
      exercised by the Meta adapter's `verifyCredentials`, Issue #644).
      Producer: `social-account-directory.ts`'s
      `recordSocialAccountVerification`, called from
      `POST /api/v1/social-publishing/accounts/{id}/verify`.
  awcms-mini.social-publishing.account.verification-failed:
    address: awcms-mini.social-publishing.account.verification-failed
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A connected social account's verification check failed (e.g. the bot
      lacks channel-administrator permission, or a Meta token has expired/
      lost a required scope) (Issue #646). Informational — does NOT itself
      change `connection_status`/`auto_publish_enabled`. Producer:
      `social-account-directory.ts`'s `recordSocialAccountVerification`.
  awcms-mini.social-publishing.rule.created:
    address: awcms-mini.social-publishing.rule.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A social publish rule was created (Issue #643). Producer:
      `social-publish-rule-directory.ts`'s `createSocialPublishRule`.
  awcms-mini.social-publishing.rule.updated:
    address: awcms-mini.social-publishing.rule.updated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A social publish rule was updated (Issue #643). Producer:
      `social-publish-rule-directory.ts`'s `updateSocialPublishRule`.
  awcms-mini.social-publishing.rule.deleted:
    address: awcms-mini.social-publishing.rule.deleted
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A social publish rule was soft-deleted (Issue #643). Producer:
      `social-publish-rule-directory.ts`'s `softDeleteSocialPublishRule`.
  awcms-mini.social-publishing.job.created:
    address: awcms-mini.social-publishing.job.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      An outbox job was created after an eligible article publish event
      (Issue #643 acceptance criterion: "Publishing jobs are created after
      eligible article publish event"). Idempotent per
      article/account/action (deterministic idempotency key). Producer:
      `social-publishing/application/create-social-publish-jobs.ts`'s
      `createSocialPublishJobsForArticle`, invoked by `blog_content`'s
      publish route/scheduled-publish worker via `SocialPublishingPort`.
  awcms-mini.social-publishing.job.approved:
    address: awcms-mini.social-publishing.job.approved
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A job pending approval was approved and may now be dispatched to its
      provider (Issue #643). Producer:
      `social-publish-job-directory.ts`'s `approveSocialPublishJob`.
  awcms-mini.social-publishing.job.cancelled:
    address: awcms-mini.social-publishing.job.cancelled
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A job was cancelled (Issue #643), from any non-terminal-success
      status. Producer: `social-publish-job-directory.ts`'s
      `cancelSocialPublishJob`.
  awcms-mini.social-publishing.job.retry-requested:
    address: awcms-mini.social-publishing.job.retry-requested
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A manual retry was requested for a failed/rate-limited/needs-reauth
      job (Issue #643) — `attemptCount` is not reset. Producer:
      `social-publish-job-directory.ts`'s `retrySocialPublishJob`.
  awcms-mini.social-publishing.job.published:
    address: awcms-mini.social-publishing.job.published
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A job was successfully published to its provider (Issue #643) —
      `externalPostId`/`externalPostUrl` now set. This foundation issue
      ships zero real provider adapters, so this event is only ever
      producible once an adapter (#644/#645/#646) is registered. Producer:
      `social-publishing/application/social-publish-dispatch.ts`'s
      `dispatchSocialPublishQueue`.
  awcms-mini.social-publishing.job.publish-failed:
    address: awcms-mini.social-publishing.job.publish-failed
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A publish attempt failed but is still eligible for exponential
      backoff retry (Issue #643). Producer: `social-publish-dispatch.ts`.
  awcms-mini.social-publishing.job.publish-failed-terminal:
    address: awcms-mini.social-publishing.job.publish-failed-terminal
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A job permanently failed after exhausting its retry budget (Issue
      #643 acceptance criterion: "terminal failed state"). Producer:
      `social-publish-dispatch.ts`.
  awcms-mini.social-publishing.job.rate-limited:
    address: awcms-mini.social-publishing.job.rate-limited
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A provider reported a rate limit for a publish attempt (Issue #643)
      — backoff seeded from the provider's own `retryAfterSeconds` hint
      when present. Producer: `social-publish-dispatch.ts`.
  awcms-mini.social-publishing.job.needs-reauth:
    address: awcms-mini.social-publishing.job.needs-reauth
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A provider reported an expired/invalid token for a publish attempt
      (Issue #643 acceptance criterion: "Support reauthorization flow when
      token expires") — the linked account is also flipped to
      `needs_reauth`. Producer: `social-publish-dispatch.ts`.
  awcms-mini.domain-event-runtime.sample.recorded:
    address: awcms-mini.domain-event-runtime.sample.recorded
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      Reference/example event (Issue #742, epic `platform-evolution` #738)
      used to exercise the domain-event-runtime outbox, dispatcher,
      ordering, retry/backoff, dead-letter, and replay mechanism end-to-end.
      Real producer modules publish their OWN event types the same way, via
      `appendDomainEvent` — this one is intentionally self-contained rather
      than tied to another module's business logic in this foundation
      issue (see `src/modules/domain-event-runtime/domain/event-type-
      registry.ts`'s own doc comment). Producer: any caller of
      `application/append-domain-event.ts`'s `appendDomainEvent` for this
      event type (test fixtures in this issue); consumers:
      `infrastructure/consumer-registry.ts`'s two reference consumers.
  awcms-mini.workflow.instance.started:
    address: awcms-mini.workflow.instance.started
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A workflow instance was started, pinned to the currently-active
      workflow definition version (Issue #747, epic `platform-evolution`
      #738). Producer: `workflow-approval/application/workflow-instance.ts`'s
      `startWorkflowInstance`, via `appendDomainEvent` in the same
      transaction as the instance's creation.
  awcms-mini.workflow.instance.advanced:
    address: awcms-mini.workflow.instance.advanced
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A workflow instance's active task was decided (or
      force-decided) and the instance advanced to its next node(s),
      without yet reaching a terminal outcome (Issue #747). Producer:
      `workflow-approval/application/workflow-instance-decision.ts`'s
      `completeApprovalTaskAndAdvance`.
  awcms-mini.workflow.instance.approved:
    address: awcms-mini.workflow.instance.approved
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A workflow instance reached an `end` node with outcome `approved`
      (Issue #747). Producer: `workflow-approval/application/
      workflow-instance.ts`/`workflow-instance-decision.ts`.
  awcms-mini.workflow.instance.rejected:
    address: awcms-mini.workflow.instance.rejected
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A workflow instance reached an `end` node with outcome `rejected`,
      or was force-rejected (Issue #747). Producer: `workflow-approval/
      application/workflow-instance.ts`/`workflow-instance-decision.ts`.
  awcms-mini.workflow.instance.cancelled:
    address: awcms-mini.workflow.instance.cancelled
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      An administrator cancelled a running workflow instance (Issue #747).
      Producer: `workflow-approval/application/workflow-recovery.ts`'s
      `cancelWorkflowInstance`, via `POST /api/v1/workflows/instances/{id}/
      cancel`.
  awcms-mini.workflow.task.escalated:
    address: awcms-mini.workflow.task.escalated
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A pending workflow task passed its due date and was escalated by the
      scheduled escalation/timeout job (Issue #747). Producer:
      `workflow-approval/application/workflow-escalation.ts`'s
      `escalateDueTasksForTenant`, run via
      `bun run workflow:escalations:dispatch`.
  awcms-mini.workflow.delegation.created:
    address: awcms-mini.workflow.delegation.created
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A workflow delegation/substitute assignment was created (Issue
      #747). Producer: `workflow-approval/application/
      workflow-delegation-directory.ts`'s `createWorkflowDelegation`.
  awcms-mini.workflow.delegation.revoked:
    address: awcms-mini.workflow.delegation.revoked
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      A workflow delegation/substitute assignment was revoked (Issue
      #747). Producer: `workflow-approval/application/
      workflow-delegation-directory.ts`'s `revokeWorkflowDelegation`.
  awcms-mini.profile-identity.profile.merged:
    address: awcms-mini.profile-identity.profile.merged
    messages:
      DomainEvent:
        $ref: "#/components/messages/DomainEvent"
    description: >-
      Published when a profile merge request is executed (Issue #748,
      epic `platform-evolution` #738 Wave 2): the loser profile is
      soft-deleted (`merged_into_profile_id` set) and its
      `awcms_mini_profile_entity_links` rows are repointed to the
      survivor. Payload: `mergeRequestId`, `survivorProfileId`,
      `loserProfileId`, `entityLinksRepointedCount`. Lets domain modules
      react to the merge mapping through the outbox instead of importing
      `profile_identity` tables directly (see
      `src/modules/_shared/ports/party-directory-port.ts` for the
      pull-based equivalent). Producer:
      `profile-identity/application/merge-workflow.ts`'s
      `executeMergeRequest`.
operations:
  publishSyncPushRequested:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.sync.push.requested"
    messages:
      - $ref: "#/channels/awcms-mini.sync.push.requested/messages/DomainEvent"
  publishDatabasePoolSaturated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.database.pool.saturated"
    messages:
      - $ref: "#/channels/awcms-mini.database.pool.saturated/messages/DomainEvent"
  publishDatabasePoolRejected:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.database.pool.rejected"
    messages:
      - $ref: "#/channels/awcms-mini.database.pool.rejected/messages/DomainEvent"
  publishEmailMessageQueued:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.email.message.queued"
    messages:
      - $ref: "#/channels/awcms-mini.email.message.queued/messages/DomainEvent"
  publishEmailMessageSent:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.email.message.sent"
    messages:
      - $ref: "#/channels/awcms-mini.email.message.sent/messages/DomainEvent"
  publishEmailMessageFailed:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.email.message.failed"
    messages:
      - $ref: "#/channels/awcms-mini.email.message.failed/messages/DomainEvent"
  publishEmailMessageSuppressed:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.email.message.suppressed"
    messages:
      - $ref: "#/channels/awcms-mini.email.message.suppressed/messages/DomainEvent"
  publishEmailMessageCancelled:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.email.message.cancelled"
    messages:
      - $ref: "#/channels/awcms-mini.email.message.cancelled/messages/DomainEvent"
  publishBlogContentPostCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.created"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.created/messages/DomainEvent"
  publishBlogContentPostUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.updated/messages/DomainEvent"
  publishBlogContentPostSubmittedForReview:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.submitted-for-review"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.submitted-for-review/messages/DomainEvent"
  publishBlogContentPostPublished:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.published"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.published/messages/DomainEvent"
  publishBlogContentPostScheduled:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.scheduled"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.scheduled/messages/DomainEvent"
  publishBlogContentPostArchived:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.archived"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.archived/messages/DomainEvent"
  publishBlogContentPostDeleted:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.deleted"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.deleted/messages/DomainEvent"
  publishBlogContentPostRestored:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.restored"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.restored/messages/DomainEvent"
  publishBlogContentPostPurged:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.post.purged"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.post.purged/messages/DomainEvent"
  publishBlogContentRevisionCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.revision.created"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.revision.created/messages/DomainEvent"
  publishBlogContentTermCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.term.created"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.term.created/messages/DomainEvent"
  publishBlogContentTermUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.term.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.term.updated/messages/DomainEvent"
  publishBlogContentSettingsUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.settings.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.settings.updated/messages/DomainEvent"
  publishBlogContentTemplateCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.template.created"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.template.created/messages/DomainEvent"
  publishBlogContentTemplateUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.template.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.template.updated/messages/DomainEvent"
  publishBlogContentTemplateDeleted:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.template.deleted"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.template.deleted/messages/DomainEvent"
  publishBlogContentMenuCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.menu.created"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.menu.created/messages/DomainEvent"
  publishBlogContentMenuUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.menu.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.menu.updated/messages/DomainEvent"
  publishBlogContentMenuDeleted:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.menu.deleted"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.menu.deleted/messages/DomainEvent"
  publishBlogContentWidgetCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.widget.created"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.widget.created/messages/DomainEvent"
  publishBlogContentWidgetUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.widget.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.widget.updated/messages/DomainEvent"
  publishBlogContentWidgetDeleted:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.widget.deleted"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.widget.deleted/messages/DomainEvent"
  publishBlogContentAdCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.ad.created"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.ad.created/messages/DomainEvent"
  publishBlogContentAdUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.ad.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.ad.updated/messages/DomainEvent"
  publishBlogContentAdDeleted:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.ad.deleted"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.ad.deleted/messages/DomainEvent"
  publishBlogContentThemeUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.theme.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.theme.updated/messages/DomainEvent"
  publishBlogContentInternalTagLinkingPolicyUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.blog-content.internal-tag-linking-policy.updated"
    messages:
      - $ref: "#/channels/awcms-mini.blog-content.internal-tag-linking-policy.updated/messages/DomainEvent"
  publishSocialPublishingAccountConnected:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.account.connected"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.account.connected/messages/DomainEvent"
  publishSocialPublishingAccountDisconnected:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.account.disconnected"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.account.disconnected/messages/DomainEvent"
  publishSocialPublishingAccountNeedsReauth:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.account.needs-reauth"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.account.needs-reauth/messages/DomainEvent"
  publishSocialPublishingAccountVerified:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.account.verified"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.account.verified/messages/DomainEvent"
  publishSocialPublishingAccountVerificationFailed:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.account.verification-failed"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.account.verification-failed/messages/DomainEvent"
  publishSocialPublishingRuleCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.rule.created"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.rule.created/messages/DomainEvent"
  publishSocialPublishingRuleUpdated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.rule.updated"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.rule.updated/messages/DomainEvent"
  publishSocialPublishingRuleDeleted:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.rule.deleted"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.rule.deleted/messages/DomainEvent"
  publishSocialPublishingJobCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.created"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.created/messages/DomainEvent"
  publishSocialPublishingJobApproved:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.approved"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.approved/messages/DomainEvent"
  publishSocialPublishingJobCancelled:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.cancelled"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.cancelled/messages/DomainEvent"
  publishSocialPublishingJobRetryRequested:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.retry-requested"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.retry-requested/messages/DomainEvent"
  publishSocialPublishingJobPublished:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.published"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.published/messages/DomainEvent"
  publishSocialPublishingJobPublishFailed:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.publish-failed"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.publish-failed/messages/DomainEvent"
  publishSocialPublishingJobPublishFailedTerminal:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.publish-failed-terminal"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.publish-failed-terminal/messages/DomainEvent"
  publishSocialPublishingJobRateLimited:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.rate-limited"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.rate-limited/messages/DomainEvent"
  publishSocialPublishingJobNeedsReauth:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.social-publishing.job.needs-reauth"
    messages:
      - $ref: "#/channels/awcms-mini.social-publishing.job.needs-reauth/messages/DomainEvent"
  publishDomainEventRuntimeSampleRecorded:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.domain-event-runtime.sample.recorded"
    messages:
      - $ref: "#/channels/awcms-mini.domain-event-runtime.sample.recorded/messages/DomainEvent"
  subscribeDomainEventRuntimeSampleRecorded:
    action: receive
    channel:
      $ref: "#/channels/awcms-mini.domain-event-runtime.sample.recorded"
    messages:
      - $ref: "#/channels/awcms-mini.domain-event-runtime.sample.recorded/messages/DomainEvent"
  publishWorkflowInstanceStarted:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.workflow.instance.started"
    messages:
      - $ref: "#/channels/awcms-mini.workflow.instance.started/messages/DomainEvent"
  publishWorkflowInstanceAdvanced:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.workflow.instance.advanced"
    messages:
      - $ref: "#/channels/awcms-mini.workflow.instance.advanced/messages/DomainEvent"
  publishWorkflowInstanceApproved:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.workflow.instance.approved"
    messages:
      - $ref: "#/channels/awcms-mini.workflow.instance.approved/messages/DomainEvent"
  publishWorkflowInstanceRejected:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.workflow.instance.rejected"
    messages:
      - $ref: "#/channels/awcms-mini.workflow.instance.rejected/messages/DomainEvent"
  publishWorkflowInstanceCancelled:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.workflow.instance.cancelled"
    messages:
      - $ref: "#/channels/awcms-mini.workflow.instance.cancelled/messages/DomainEvent"
  publishWorkflowTaskEscalated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.workflow.task.escalated"
    messages:
      - $ref: "#/channels/awcms-mini.workflow.task.escalated/messages/DomainEvent"
  publishWorkflowDelegationCreated:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.workflow.delegation.created"
    messages:
      - $ref: "#/channels/awcms-mini.workflow.delegation.created/messages/DomainEvent"
  publishWorkflowDelegationRevoked:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.workflow.delegation.revoked"
    messages:
      - $ref: "#/channels/awcms-mini.workflow.delegation.revoked/messages/DomainEvent"
  publishProfileIdentityProfileMerged:
    action: send
    channel:
      $ref: "#/channels/awcms-mini.profile-identity.profile.merged"
    messages:
      - $ref: "#/channels/awcms-mini.profile-identity.profile.merged/messages/DomainEvent"
components:
  securitySchemes:
    syncHmac:
      type: httpApiKey
      in: header
      name: X-AWCMS-Mini-Signature
      description: HMAC signature paired with X-AWCMS-Mini-Node-ID and X-AWCMS-Mini-Timestamp.
  messages:
    DomainEvent:
      name: DomainEvent
      title: AWCMS-Mini domain event envelope
      payload:
        $ref: "#/components/schemas/DomainEventEnvelope"
      headers:
        type: object
        required:
          - X-AWCMS-Mini-Node-ID
          - X-AWCMS-Mini-Timestamp
          - X-AWCMS-Mini-Signature
        properties:
          X-AWCMS-Mini-Node-ID:
            type: string
          X-AWCMS-Mini-Timestamp:
            type: string
            format: date-time
          X-AWCMS-Mini-Signature:
            type: string
  schemas:
    DomainEventEnvelope:
      type: object
      required:
        - event_id
        - event_type
        - occurred_at
        - producer
        - payload
      additionalProperties: false
      properties:
        event_id:
          type: string
          format: uuid
        event_type:
          type: string
          pattern: "^[a-z0-9]+(\\.[a-z0-9_]+)+$"
        occurred_at:
          type: string
          format: date-time
        producer:
          type: object
          required:
            - service
            - module
          additionalProperties: false
          properties:
            service:
              type: string
              const: awcms-mini
            module:
              type: string
        tenant_id:
          type:
            - string
            - "null"
          format: uuid
        correlation_id:
          type:
            - string
            - "null"
        payload:
          type: object
          additionalProperties: true
